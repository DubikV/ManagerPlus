<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                tools:context="com.gmail.vanyadubik.managerplus.activity.ImageActivity">

    <com.gmail.vanyadubik.managerplus.ui.TouchImageView
              android:id="@+id/image"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_gravity="center"
              android:gravity="center"
              android:scaleType="fitCenter"/>

    <android.support.design.widget.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        app:fabSize="normal"
        app:srcCompat="@drawable/ic_arrow_back"
        android:id="@+id/return_image"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="@dimen/marging_button_map"
        android:layout_marginBottom="@dimen/marging_button_map"/>

    <SeekBar
        android:id="@+id/seekBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="1dp"
        android:max="100"
        android:layout_marginStart="@dimen/seekbar_marging"
        android:layout_marginEnd="@dimen/seekbar_marging"
        android:paddingStart="@dimen/seekbar_padding"
        android:paddingEnd="@dimen/seekbar_padding"
        android:thumb="@drawable/thumb_image"
        android:layout_alignBottom="@+id/return_image"
        android:layout_toEndOf="@+id/return_image" />


</RelativeLayout>